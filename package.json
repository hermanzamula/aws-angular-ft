{
  "name": "aws-angular-ft",
  "version": "1.0.0",
  "description": "Fault-Tolerant System With Node Angular AWS",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "deploy": "cross-env-shell deploy.sh",
    "start": "cd client && npm run start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "prettier": "^3.5.3",
    "shx": "^0.4.0",
    "typescript": "^5.8.2"
  }
}
